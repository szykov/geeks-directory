import { createAction } from '@ngrx/store';

export const restore = createAction('[Auth] Restore Login');
export const logout = createAction('[Auth] Logout');
export const personalize = createAction('[Auth] Personalize');
